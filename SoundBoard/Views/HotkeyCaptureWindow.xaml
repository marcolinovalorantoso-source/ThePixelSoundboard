<Window x:Class="SoundBoard.Views.HotkeyCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Assegna hotkey" Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1B1D23" Foreground="#F1F2F6"
        PreviewKeyDown="Window_PreviewKeyDown">
    <StackPanel Margin="20">
        <TextBlock Text="Premi la combinazione di tasti desiderata (es. Ctrl+Alt+F1):"
                   TextWrapping="Wrap" Margin="0,0,0,12"/>
        <Border Background="{StaticResource BgElevatedBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,16">
            <TextBlock x:Name="GestureText" Text="Nessuna combinazione" FontSize="16" FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Rimuovi" Width="90" Margin="0,0,8,0" Click="Remove_Click"/>
            <Button Content="Annulla" Width="90" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Salva" Width="90" Background="{StaticResource AccentBrush}" Click="Save_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
